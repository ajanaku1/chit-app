# Local build notes
*.done-when.md
ai/
landing-brief.md

# secrets — never committed, not even a testnet burn key
.env
.env.*
!.env.example

# build output. The landing's source lives in landing/public and is tracked;
# only the assembled root public/ is output.
/public/
node_modules/
artifacts/
cache/
dist/
dist-fleet/
build/
types/
typechain-types/
app/dist/
app/node_modules/
video/

# Local tooling
*.mov
.DS_Store
*.log
.vercel/
.codex/

app/evidence/*
!app/evidence/*-1440.png
!app/evidence/*-390.png
# The poll runner's on-disk store (BOT_PGLITE_DIR)
.bot-data/

# Confidential review material: never tracked, never mirrored. The public
# mirror force-pushes from main, so anything committed here is published.
/chit-review.zip
/chit-review/
/chit-fleet-audit.pdf
/chit-deck.pdf
/chit-fleet-*.pdf
/chit-wallet-reconnect-fix.md
